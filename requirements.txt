# Signal Extraction API Requirements
# Install with: pip install -r requirements.txt --break-system-packages

# Core FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# HTTP client for testing
httpx==0.26.0

# For CORS (React frontend later)
python-multipart==0.0.6

# XML parsing (ISOXML support)
lxml==5.1.0

# Optional: Better JSON handling
orjson==3.9.12

# Ontology and RDF support
rdflib==7.0.0  # For ontology mapping layer

# Optional: For future knowledge graph storage
# neo4j==5.16.0  # Uncomment when ready for knowledge graph

# Development/Testing (optional)
# pytest==7.4.3
# pytest-asyncio==0.23.2
